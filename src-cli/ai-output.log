OpenAI Codex v0.115.0 (research preview)
--------
workdir: /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: none
reasoning summaries: none
session id: 019d24da-47eb-7cf3-b5fc-7be87eb65b3d
--------
user
Implement the spec.
mcp startup: no servers
codex
I’ll locate the implementation spec in this repo, then map the required changes and apply them directly in code. I’m starting by scanning for spec files and project structure.
exec
/bin/zsh -lc 'ls -la' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
total 56
drwxr-xr-x@  9 zsvedic  staff   288 Mar 25 12:46 .
drwxr-xr-x@  6 zsvedic  staff   192 Mar 24 16:18 ..
-rw-r--r--@  1 zsvedic  staff  6148 Mar 23 21:15 .DS_Store
-rw-r--r--@  1 zsvedic  staff   542 Mar 25 12:56 ai-output.log
-rwxr-xr-x@  1 zsvedic  staff   186 Mar 20 12:46 implement.sh
-rw-r--r--@  1 zsvedic  staff    37 Mar 23 15:03 journal.md
-rw-r--r--@  1 zsvedic  staff  2041 Mar 25 12:55 spec.md
-rwxr-xr-x@  1 zsvedic  staff  3820 Mar 24 18:42 stirr.py
drwxr-xr-x@ 12 zsvedic  staff   384 Mar 24 18:40 tests
exec
/bin/zsh -lc 'rg --files' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cliexec
/bin/zsh -lc 'rg -n "spec|requirements|todo|TODO|IMPLEMENT|feature" -S .' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
spec.md
journal.md
ai-output.log
tests/test4-dry-run.sh
tests/test1-run.sh
tests/test1-run.log
tests/test4-dry-run.log
tests/test-dir/test-hashtags.txt
tests/test-dir/#AI.#Test
tests/test3-help.sh
tests/test3-help.log
tests/test2-mock.py
tests/test2-mock.log
stirr.py
implement.sh
 succeeded in 0ms:
./ai-output.log:13:Implement the spec.
./ai-output.log:16:I’ll locate the implementation spec in this repo, then map the required changes and apply them directly in code. I’m starting by scanning for spec files and project structure.
./implement.sh:8:  "Implement the spec." \
codex
I found `spec.md`; next I’ll read the spec plus current CLI/tests to implement exactly what’s missing, then run the test scripts.
exec
/bin/zsh -lc "sed -n '1,260p' spec.md" in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cliexec
/bin/zsh -lc "sed -n '1,260p' stirr.py" in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
--
tags: #Human
--

## General
- Read [#STIRR](../STIRR-rules.md). 
- Implement the `stirr.py` script as: 
  - py with python3 shebang.
  - Just py stdlib.
  - Use a py `__main__` check.
- Example:
```console
> ./stirr.py
FILE TREE:
. 19.62KB
  STIRR-rules.md 6.71KB #Human (7#HC 6#FooBar 3#Human 2#AI)
  scripts/ 0.14KB
    test-file.txt 0.12KB #human (...
...
TAG TOTALS: 
 succeeded in 0ms:
#!/usr/bin/env python3
import glob, os, re, sys
from collections import Counter

TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
MAX_TEXT_FILE_SIZE = 128 * 1024
HELP_TEXT = """USAGE:
  stirr.py [--dry-run] PATH1 [PATH2 ...]
  stirr.py --help

  <PATHx> ...
      A file(s)/dir(s) to check.
"""


def kb(n): return f"{n / 1024:.2f}KB"
def fmt(items): return " ".join(f"{v}{k}" for k, v in items)

def top(cnt, order=None, n=5):
exec
/bin/zsh -lc "for f in tests/*.sh tests/*.py; do echo '===== '\""'$f"; sed -n '"'1,260p' \""'$f"; done' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
===== tests/test1-run.sh
#!/usr/bin/env bash
# #Human
set -euo pipefail

trap 'echo FAIL; exit 1' ERR

cd "$(dirname "$0")/.."
./stirr.py > tests/test1-run.log 2>&1
echo Pass
===== tests/test3-help.sh
#!/usr/bin/env bash
# #Human
set -euo pipefail

trap 'echo FAIL; exit 1' ERR

cd "$(dirname "$0")/.."

{
codex
I’ve identified the missing Iteration 3 requirement: `NLOC` and `LexTok` must match the tokenizer logic in `../src-lloc/regex-tokenizer.py`. I’ll pull that exact logic and wire it into `get_text_file_info` output formatting.
exec
/bin/zsh -lc "sed -n '1,320p' ../src-lloc/regex-tokenizer.py" in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
#!/usr/bin/env python3
import re, os
# #Human
# A regex tokenizer that counts the # of lexical tokens / NLOC (non-empty lines) in each text file.

# Test online: https://regexr.com/8ldco
RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')

print(f"{'NLOC':<5} | {'SumToken':<8} | File")

for dp,_,fs in os.walk('.'):
    for f in fs:
        p = os.path.join(dp,f)
        try:
            s = open(p,encoding='utf-8',errors='ignore').read()
        except:
            continue
        lines = [l for l in s.splitlines() if l.strip()]
        tokens = RE.findall(s)
        print(f"{len(lines):<5} | {len(tokens):<8} | {p}")
codex
I’m now updating `stirr.py` to add exact tokenizer-based `get_nloc_lextokens(txt)` and a true hierarchical tree print with per-directory aggregated sizes from parsed files. After patching, I’ll run all provided tests.
file update
M /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli/stirr.py
@@ -5,2 +5,3 @@
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
@@ -24,2 +25,8 @@
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
@@ -44,2 +51,3 @@
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
@@ -49,3 +57,3 @@
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
@@ -55,4 +63,5 @@
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
@@ -68,10 +77,22 @@
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
 
@@ -79,6 +100,34 @@
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
@@ -86,5 +135,7 @@
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
apply_patch(auto_approved=true) exited 0 in 22ms:
Success. Updated the following files:
M stirr.py
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..cd9142e7f2941f92f259d055e223b8995c0dc83b
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..cd9142e7f2941f92f259d055e223b8995c0dc83b
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

codex
Edits are in place. I’m running the full test set now and will fix anything that fails.
exec
/bin/zsh -lc 'cd /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli && ./tests/test1-run.sh && ./tests/test2-mock.py && ./tests/test3-help.sh && ./tests/test4-dry-run.sh' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli exited 1 in 17ms:
FAIL
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..cd9142e7f2941f92f259d055e223b8995c0dc83b
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

codex
`test2-mock.py` failed; I’m checking `tests/test2-mock.log` to identify the assertion mismatch and patch accordingly.
exec
/bin/zsh -lc "sed -n '1,260p' tests/test2-mock.log" in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
RULE TAGS: #TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL
FILE TREE:
tmptdi5b856/ 0.03KB
  a.md 0.01KB #Foo (1 NLOC, 6 LexTok) (2#foo 1#bar)
  sub/ 0.01KB
    b.txt 0.01KB #foo-bar (1 NLOC, 6 LexTok) (1#foo-bar 1#foo)
TAG TOTALS:
3#foo 1#bar 1#foo-bar
API KEYS:
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

ERROR: 
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..cd9142e7f2941f92f259d055e223b8995c0dc83b
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

file update
M /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli/stirr.py
@@ -96,3 +96,3 @@
     pfx = " " * indent
-    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'} ({fi['nloc']} NLOC, {fi['lextok']} LexTok){y}")
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
apply_patch(auto_approved=true) exited 0 in 34ms:
Success. Updated the following files:
M stirr.py
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..c9cbc03ba3371d87b4e3783512b137c1579d3f8e
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..c9cbc03ba3371d87b4e3783512b137c1579d3f8e
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

codex
I adjusted formatting to retain backward-compatible tag substring expectations while still showing `NLOC` and `LexTok`. Re-running the full tests now.
exec
/bin/zsh -lc 'cd /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli && ./tests/test1-run.sh && ./tests/test2-mock.py && ./tests/test3-help.sh && ./tests/test4-dry-run.sh' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 57ms:
Pass
Pass
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..c9cbc03ba3371d87b4e3783512b137c1579d3f8e
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

exec
/bin/zsh -lc 'git -C /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli diff -- stirr.py' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 0ms:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349..c9cbc03 100755
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@ import glob, os, re, sys
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@ def top(cnt, order=None, n=5):
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..c9cbc03ba3371d87b4e3783512b137c1579d3f8e
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,69 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

codex
All tests pass. I’m doing one small cleanup (remove an unused local variable in the tree printer) and then I’ll summarize exactly what changed.
file update
M /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli/stirr.py
@@ -101,3 +101,2 @@
     def print_dir(root, rel_dir, indent):
-        abs_dir = root if rel_dir == "." else os.path.join(root, rel_dir)
         direct_files = []
apply_patch(auto_approved=true) exited 0 in 34ms:
Success. Updated the following files:
M stirr.py
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..548dd4f9f96f8e5804b97afcaa0cad024d3af92d
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,68 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..548dd4f9f96f8e5804b97afcaa0cad024d3af92d
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,68 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

exec
/bin/zsh -lc 'cd /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli && ./tests/test1-run.sh && ./tests/test2-mock.py && ./tests/test3-help.sh && ./tests/test4-dry-run.sh' in /Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli succeeded in 42ms:
Pass
Pass
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..548dd4f9f96f8e5804b97afcaa0cad024d3af92d
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,68 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

codex
Implemented the spec in [stirr.py](/Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli/stirr.py).

Changes made:
- Added exact Iteration 3 tokenizer regex and `get_nloc_lextokens(txt)` logic matching `../src-lloc/regex-tokenizer.py`.
- Extended per-file info with `nloc` and `lextok`.
- Updated file output to display `NLOC` and `LexTok` for each parsed text file.
- Improved `traverse` to compute per-directory aggregated sizes from parsed files.
- Updated `print_file_tree` to print a hierarchical tree with directory sizes (sum of parsed files in that folder/subtree).
- Kept existing behavior for hashtag detection, top-5 per file, totals, usage/help, dry-run, and API key printing.

Validation:
- Ran all provided tests successfully:
  - `tests/test1-run.sh` -> Pass
  - `tests/test2-mock.py` -> Pass
  - `tests/test3-help.sh` -> Pass
  - `tests/test4-dry-run.sh` -> Pass
file update:
diff --git a/STIRR/src-cli/stirr.py b/STIRR/src-cli/stirr.py
index fa0e349e62a726dc16f0aafc86d7eea80ef65bb9..548dd4f9f96f8e5804b97afcaa0cad024d3af92d
--- a/STIRR/src-cli/stirr.py
+++ b/STIRR/src-cli/stirr.py
@@ -3,6 +3,7 @@
 from collections import Counter
 
 TAG_RE = re.compile(r"(?<!\w)#[A-Za-z][\w-]*")
+LEXTOK_RE = re.compile(r'"(\\.|[^"])*"|\'(\\.|[^\'])*\'|\w+|==|!=|<=|>=|->|[{}()\[\];,]|[^\s]')
 RULE_TAGS = "#TextRL #ConventionRL #SDD #TDD #ImplementRL #HITL #RepeatRL"
 MAX_TEXT_FILE_SIZE = 128 * 1024
 HELP_TEXT = """USAGE:
@@ -22,6 +23,12 @@
     return sorted(cnt.items(), key=lambda kv: (-kv[1], order.get(kv[0], 10**9), kv[0]))[:n]
 
 
+def get_nloc_lextokens(txt):
+    lines = [l for l in txt.splitlines() if l.strip()]
+    tokens = LEXTOK_RE.findall(txt)
+    return (len(lines), len(tokens))
+
+
 def get_file_text_hashtag(path):
     try:
         with open(path, "rb") as f: chunk = f.read(4096)
@@ -42,19 +49,21 @@
         with open(path, "r", encoding="utf-8", errors="ignore") as f: txt = f.read()
     except OSError:
         return None
+    nloc, lextok = get_nloc_lextokens(txt)
     tags = [t.lower() for t in TAG_RE.findall(txt)]
     order = {}
     for t in tags: order.setdefault(t, len(order))
     return {
         "path": os.path.abspath(path), "name": os.path.basename(path), "size": size,
-        "first_tag": first, "counts": Counter(tags), "order": order,
+        "first_tag": first, "counts": Counter(tags), "order": order, "nloc": nloc, "lextok": lextok,
     }
 
 
 def traverse(paths):
     roots = [paths] if isinstance(paths, str) else list(paths)
-    files, totals = [], Counter()
+    files, totals, dirs = [], Counter(), {}
     for root in roots:
+        root_abs = os.path.abspath(root)
         if os.path.isfile(root):
             items = [root]
         elif os.path.isdir(root):
@@ -66,27 +75,68 @@
             fi = get_text_file_info(p)
             if not fi: continue
             files.append(fi); totals.update(fi["counts"])
+            if os.path.isfile(root):
+                continue
+            d = os.path.dirname(fi["path"])
+            while True:
+                dirs[d] = dirs.get(d, 0) + fi["size"]
+                if d == root_abs:
+                    break
+                nd = os.path.dirname(d)
+                if nd == d or not d.startswith(root_abs + os.sep):
+                    break
+                d = nd
     files.sort(key=lambda f: f["path"])
-    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": {}, "totals": totals}
+    return {"roots": [os.path.abspath(p) for p in roots], "files": files, "dirs": dirs, "totals": totals}
 
 
-def print_file_info(fi):
+def print_file_info(fi, indent=2):
     x = top(fi["counts"], fi["order"], 5)
     y = f" ({fmt(x)})" if x else ""
-    print(f"  {fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y}")
+    pfx = " " * indent
+    print(f"{pfx}{fi['name']} {kb(fi['size'])} {fi['first_tag'] or '-'}{y} ({fi['nloc']} NLOC, {fi['lextok']} LexTok)")
 
 
 def print_file_tree(data):
+    def print_dir(root, rel_dir, indent):
+        direct_files = []
+        direct_dirs = set()
+        for f in by_root.get(root, []):
+            rel = os.path.relpath(f["path"], root)
+            if rel.startswith(".."):
+                continue
+            d = os.path.dirname(rel) or "."
+            if d == rel_dir:
+                direct_files.append(f)
+            elif d.startswith(rel_dir + os.sep) if rel_dir != "." else d != ".":
+                nxt = d.split(os.sep)[0] if rel_dir == "." else d[len(rel_dir) + 1:].split(os.sep)[0]
+                direct_dirs.add((nxt if rel_dir == "." else rel_dir + os.sep + nxt))
+        for f in sorted(direct_files, key=lambda x: x["path"]):
+            print_file_info(f, indent)
+        for d in sorted(direct_dirs):
+            name = os.path.basename(d)
+            print(f"{' ' * indent}{name}/ {kb(data['dirs'].get(os.path.join(root, d), 0))}")
+            print_dir(root, d, indent + 2)
+
     print("FILE TREE:")
+    by_root = {r: [] for r in data["roots"]}
+    for f in data["files"]:
+        for r in data["roots"]:
+            if os.path.isfile(r):
+                if f["path"] == r:
+                    by_root[r].append(f)
+            elif f["path"].startswith(r + os.sep):
+                by_root[r].append(f)
+                break
     for root in data["roots"]:
-        rp = root + os.sep
-        inside = [f for f in data["files"] if f["path"] == root or f["path"].startswith(rp)]
         lbl = os.path.basename(root.rstrip(os.sep)) or root
         if os.path.isfile(root):
             print(f"{lbl} {kb(os.path.getsize(root) if os.path.exists(root) else 0)}")
+            for f in by_root.get(root, []):
+                print_file_info(f)
         else:
-            print(f"{lbl}/ {kb(sum(f['size'] for f in inside))}")
-            for f in inside: print_file_info(f)
+            print(f"{lbl}/ {kb(data['dirs'].get(root, 0))}")
+            print_dir(root, ".", 2)
 
 
 def print_hahstags(data):

tokens used
25,517
Implemented the spec in [stirr.py](/Users/zsvedic/LOCAL/CODE-2026/Inge-Zel-repo/STIRR/src-cli/stirr.py).

Changes made:
- Added exact Iteration 3 tokenizer regex and `get_nloc_lextokens(txt)` logic matching `../src-lloc/regex-tokenizer.py`.
- Extended per-file info with `nloc` and `lextok`.
- Updated file output to display `NLOC` and `LexTok` for each parsed text file.
- Improved `traverse` to compute per-directory aggregated sizes from parsed files.
- Updated `print_file_tree` to print a hierarchical tree with directory sizes (sum of parsed files in that folder/subtree).
- Kept existing behavior for hashtag detection, top-5 per file, totals, usage/help, dry-run, and API key printing.

Validation:
- Ran all provided tests successfully:
  - `tests/test1-run.sh` -> Pass
  - `tests/test2-mock.py` -> Pass
  - `tests/test3-help.sh` -> Pass
  - `tests/test4-dry-run.sh` -> Pass
